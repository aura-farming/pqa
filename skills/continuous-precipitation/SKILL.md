---
name: continuous-precipitation
description: Keep precipitating: every run names and persists its winning insight, so the registry grows continuously rather than per-session. Use when: On every collapse, across all sessions.
---

# Continuous Precipitation

## Purpose
Keep precipitating: every run names and persists its winning insight, so the registry grows continuously rather than per-session.

## When to use
On every collapse, across all sessions.

## How it fits PQA
One of PQA's three continuous loops — the precipitate side of memory; named insights compound.
It serves the loop (frame -> superpose -> collide -> collapse -> precipitate) and the governing principle: spread probability mass into divergent
and low-probability branches, then collapse it onto whatever survives attack and verification.

## Discipline
Evidence over eloquence. Name what precipitates. Report confidence honestly. Never let a
persuasive rationale substitute for a passing test.
