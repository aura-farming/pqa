---
name: bounded-exploration
description: Frame each branch as an asymmetric bet: cheap ephemeral worktree down, uncapped value up. Use when: When deciding how aggressively to explore.
---

# Bounded Exploration

## Purpose
Frame each branch as an asymmetric bet: cheap ephemeral worktree down, uncapped value up.

## When to use
When deciding how aggressively to explore.

## How it fits PQA
The rigorous reason to hunt the unknown rather than avoid it.
It serves the loop (frame -> superpose -> collide -> collapse -> precipitate) and the governing principle: spread probability mass into divergent
and low-probability branches, then collapse it onto whatever survives attack and verification.

## Discipline
Evidence over eloquence. Name what precipitates. Report confidence honestly. Never let a
persuasive rationale substitute for a passing test.
