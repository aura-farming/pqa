---
name: test-integrity
description: Locked tests and anti-gaming: distinguish pre-existing tests from branch-authored ones; never let a branch grade itself. Use when: Whenever a branch ships its own tests.
---

# Test Integrity

## Purpose
Locked tests and anti-gaming: distinguish pre-existing tests from branch-authored ones; never let a branch grade itself.

## When to use
Whenever a branch ships its own tests.

## How it fits PQA
Closes the subtlest way the verifier gets fooled.
It serves the loop (frame -> superpose -> collide -> collapse -> precipitate) and the governing principle: spread probability mass into divergent
and low-probability branches, then collapse it onto whatever survives attack and verification.

## Discipline
Evidence over eloquence. Name what precipitates. Report confidence honestly. Never let a
persuasive rationale substitute for a passing test.
