---
name: eval-harness
description: Benchmark the harness itself against a fixed task set and the single-pass baseline. Use when: Periodically, to validate continuous learning.
---

# Eval Harness

## Purpose
Benchmark the harness itself against a fixed task set and the single-pass baseline.

## When to use
Periodically, to validate continuous learning.

## How it fits PQA
Turns 'gets sharper' into a measured number.
It serves the loop (frame -> superpose -> collide -> collapse -> precipitate) and the governing principle: spread probability mass into divergent
and low-probability branches, then collapse it onto whatever survives attack and verification.

## Discipline
Evidence over eloquence. Name what precipitates. Report confidence honestly. Never let a
persuasive rationale substitute for a passing test.
