---
description: Export your learned instincts to a JSON file for sharing via scripts/instincts.py — your accumulated judgement becomes p
argument-hint: <file>
---

Export your learned instincts to a JSON file for sharing via scripts/instincts.py — your accumulated judgement becomes portable. Task: $ARGUMENTS

Hold the PQA invariant throughout: evidence over eloquence, the verifier is the source of
truth, and conviction protects exploration without exempting it from verification.
