---
description: Spawn N divergent branches only (no collapse) via pqa-generator + pqa-unknown-scout, blind to each other, then stop so t
argument-hint: <task>
---

Spawn N divergent branches only (no collapse) via pqa-generator + pqa-unknown-scout, blind to each other, then stop so the possibility space can be inspected. Task: $ARGUMENTS

Hold the PQA invariant throughout: evidence over eloquence, the verifier is the source of
truth, and conviction protects exploration without exempting it from verification.
