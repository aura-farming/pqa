---
description: Run the full loop via pqa-orchestrator: dual-frame load, superpose N divergent branches (one into the unknown), collide,
argument-hint: <task>
---

Run the full loop via pqa-orchestrator: dual-frame load, superpose N divergent branches (one into the unknown), collide, collapse on evidence, name and persist the precipitate. Task: $ARGUMENTS

Hold the PQA invariant throughout: evidence over eloquence, the verifier is the source of
truth, and conviction protects exploration without exempting it from verification.
