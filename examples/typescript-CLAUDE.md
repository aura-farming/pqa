# Example: TypeScript/Node project with PQA
Default to /pqa. Branch with pqa-typescript-brancher; verify with typescript-verification
(vitest/jest, tsc strict, eslint). Force one branch onto the refused frame (fp vs OO, runtime
validation vs types-only, event vs request). Coverage is the confidence label on every collapse.
