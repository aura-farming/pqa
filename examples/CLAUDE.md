# Example project CLAUDE.md (with PQA installed)
This project uses the PQA harness. Default to /pqa for non-trivial tasks. Hold the invariant:
nothing merges without passing the verifier; conviction explores, evidence accepts. Allocate one
branch to the unknown on every run.
