# Git workflow
Everything reaches main through a reviewed, CI-green PR — no direct pushes. Conventional commits.
`pqa/*` branches are ephemeral and machine-managed; reconcile prunes them after collapse.
